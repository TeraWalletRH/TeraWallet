NODE_ENV=
PORT=
DATABASE_URL=
RELAY_API_KEY=

# Oblivious HTTP gateway. Unset leaves it off and the routes below 404.
# Generate a key with:  bun run ohttp:key
OHTTP_PRIVATE_KEY=
OHTTP_KEY_ID=1
OHTTP_ALLOWED_PATHS=/api/agent/chat,/api/agent/propose
